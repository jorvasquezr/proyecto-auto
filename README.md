# Proyecto Final — Teoría de Autómatas

## Requirements

- Python 3.12+
- JupyterLab

Install JupyterLab if not already available:

```bash
pip install jupyterlab
```

## Setup

Start the JupyterLab server:

```bash
jupyter lab --no-browser --port=8888
```

Open your browser at `http://localhost:8888/lab`.

**Password:** `Hola1357.`
